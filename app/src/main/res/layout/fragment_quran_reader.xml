<?xml version="1.0" encoding="utf-8"?>
<!--
    fragment_quran_reader.xml — full surah reader
    Shows: surah list (default) → tap to open → ayah-by-ayah reading
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/background">

    <com.google.android.material.card.MaterialCardView
        android:id="@+id/surahHeaderCard"
        style="@style/Widget.IslamicHub.Card.Premium"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/screen_padding"
        android:visibility="gone"
        app:cardCornerRadius="@dimen/hero_card_radius">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="@dimen/card_padding_lg">

            <TextView
                android:id="@+id/surahNameArabic"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:textColor="@color/text_inverse"
                android:textSize="@dimen/text_arabic_xl"
                tools:ignore="HardcodedText"
                xmlns:tools="http://schemas.android.com/tools"
                tools:text="سورة الفاتحة" />

            <TextView
                android:id="@+id/surahNameBangla"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:layout_marginTop="@dimen/spacing_xs"
                android:textColor="@color/gold_light"
                android:textSize="@dimen/text_body_lg"
                tools:ignore="HardcodedText"
                tools:text="সূরা আল-ফাতিহা • ৭ আয়াত • মক্কী" />

        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:clipToPadding="false"
        android:paddingTop="@dimen/spacing_sm"
        android:paddingBottom="@dimen/spacing_xl" />

    <ProgressBar
        android:id="@+id/loading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:visibility="gone" />

</LinearLayout>
