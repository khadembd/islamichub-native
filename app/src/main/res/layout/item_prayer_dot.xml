<?xml version="1.0" encoding="utf-8"?>
<!-- item_prayer_dot.xml — compact prayer progress indicator -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="@dimen/spacing_xs">

    <FrameLayout
        android:layout_width="36dp"
        android:layout_height="36dp">

        <View
            android:id="@+id/prayerDotBg"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/prayer_color_dot" />

        <ImageView
            android:id="@+id/prayerCheckIcon"
            android:layout_width="16dp"
            android:layout_height="16dp"
            android:layout_gravity="center"
            android:contentDescription="@null"
            android:src="@drawable/ic_check"
            android:visibility="gone"
            app:tint="@android:color/white"
            tools:visibility="visible" />

    </FrameLayout>

    <TextView
        android:id="@+id/prayerDotName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/spacing_xs"
        android:textAppearance="@style/TextAppearance.IslamicHub.Caption"
        android:textSize="@dimen/text_caption"
        tools:text="ফজর" />

    <TextView
        android:id="@+id/prayerDotTime"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/text_tertiary"
        android:textSize="10sp"
        tools:text="৫:০৮" />

</LinearLayout>
